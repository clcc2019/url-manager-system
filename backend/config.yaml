debug: true

server:
  port: "8080"
  host: "0.0.0.0"

database:
  host: "127.0.0.1"
  port: "5432"
  user: "postgres"
  password: "postgres"
  database: "url_manager"
  ssl_mode: "disable"
  max_open_conns: 25
  max_idle_conns: 5
  conn_max_lifetime: "1h"

redis:
  address: "127.0.0.1:6379"
  password: ""
  db: 0

k8s:
  namespace: "default"
  in_cluster: false
  config_path: ""
  default_domain: "url.dslife.asia"
  ingress_class: "traefik"

security:
  jwt_secret: "your-jwt-secret-key-should-be-at-least-32-characters-long"
  allowed_images:
    - "nginx:latest"
    - "nginx:1.21"
    - "httpd:latest"
    - "httpd:2.4"
    - "registry.dslife.asia/"
  max_replicas: 3
  max_ttl_seconds: 604800  # 7 days
  default_cpu_limit: "500m"
  default_mem_limit: "512Mi"